stages:
  - build
  - security_scan
  - disk_build
  - sign

variables:
  # This creates a unique tag for every single build
  IMAGE_TAG: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  # Required for Podman on GitLab SaaS Shared Runners
  #STORAGE_DRIVER: "vfs"
  # Ensures auth persists correctly between commands
  REGISTRY_AUTH_FILE: "$CI_PROJECT_DIR/.auth.json"

  # Force Podman to use the project directory for temporary storage
  # rather than the limited /var/lib/containers/storage
  PODMAN_RUN_DIR: "$CI_PROJECT_DIR/.podman_runtime"
  PODMAN_TMPDIR: "$CI_PROJECT_DIR/.podman_tmp"
before_script:
    # Aggressively clear local runner storage before starting
    - podman system prune -a -f --volumes
    - podman rmi -a -f || true  


build_os:
  stage: build
  image: quay.io/podman/stable:latest
  script:
    - podman login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    # Diagnostic: This will fail the job immediately if variables are truly missing
    - if [ -z "${RH_REGISTRY_USER}" ]; then echo "RH_REGISTRY_USER is empty"; exit 1; fi
    - echo "Attempting login with user ${RH_REGISTRY_USER}"
    #- echo "${RH_REGISTRY_PASSWORD}" | podman login -u "${RH_REGISTRY_USER}" --password-stdin registry.redhat.io
    # Use single quotes to escape special characters in Service Account names
    - podman login -u="${RH_REGISTRY_USER}" -p="${RH_REGISTRY_PASSWORD}" registry.redhat.io
    
    # Ensure the build uses the authenticated session to pull the RHEL base
    - |
      podman build --no-cache \
        --build-arg REDHAT_ORG_ID="${REDHAT_ORG_ID}" \
        --build-arg REDHAT_ACTIVATION_KEY="${REDHAT_ACTIVATION_KEY}" \
        -t "${IMAGE_TAG}" \
        -f Containerfile.new . 
    - podman push "$IMAGE_TAG"  
compliance_scan:
  stage: security_scan
  image: quay.io/podman/stable:latest
  script:
    - podman pull --creds "$CI_REGISTRY_USER:$CI_REGISTRY_PASSWORD" "$IMAGE_TAG"
    - mkdir -p reports
    # Run the scan INSIDE the container context to bypass chroot issues
    - |
      podman run --rm -v "$(pwd)/reports:/reports:Z" "$IMAGE_TAG" /bin/bash -c "
        # Verify SCAP content is readable before scanning
        oscap info /usr/share/xml/scap/ssg/content/ssg-rhel9-ds.xml
        
        # Execute the scan using the verified RHEL 9 profile
        oscap xccdf eval \
          --profile pci-dss \
          --report /reports/audit_report.html \
          /usr/share/xml/scap/ssg/content/ssg-rhel9-ds.xml || echo 'Scan completed with findings'
      "
  artifacts:
    paths:
      - reports/audit_report.html
    when: always
    expire_in: 1 week
generate_disk:
  stage: disk_build
  image: quay.io/podman/stable:latest
  variables:
    REGISTRY_AUTH_FILE: "$CI_PROJECT_DIR/containers-auth.json"
  script:
    - mkdir -p ./output
    - podman login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    
    # 1. Pull the image to the local runner storage first
    - podman pull "$IMAGE_TAG"
    
    # 2. Run the builder with the STORAGE MOUNT
    - |
      podman run --privileged --rm \
        -v /dev:/dev \
        -v /var/lib/containers/storage:/var/lib/containers/storage \
        -v "$(pwd)/output:/output:Z" \
        -v "$REGISTRY_AUTH_FILE:/root/.docker/config.json:ro" \
        quay.io/centos-bootc/bootc-image-builder:latest \
        --type qcow2 \
        $IMAGE_TAG     
    - mv ./output/qcow2/disk.qcow2 ./fsi-hardened-v1.qcow2
  artifacts:
    paths:
      - fsi-hardened-v1.qcow2
    expire_in: 2 days
 # rules:
  #  - if: $CI_COMMIT_BRANCH == "main"

sign_os_image:
  stage: sign
  image: alpine:latest
  script:
    - apk add --no-cache cosign
    - echo "$COSIGN_KEY" > cosign.key
    # Sign and push the signature back to GitLab
    - cosign login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    - cosign sign --key cosign.key --tlog-upload=false "$IMAGE_TAG"
 # rules:
  #  - if: $CI_COMMIT_BRANCH == "main"
