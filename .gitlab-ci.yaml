stages:
  - build
  - security_scan
  - sign

variables:
  IMAGE_TAG: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA
  STORAGE_DRIVER: "vfs" # Required for nested containers on SaaS runners
  REGISTRY_AUTH_FILE: "$CI_PROJECT_DIR/.auth.json"

build_os:
  stage: build
  image: quay.io/podman/stable
  script:
    # Use CI_REGISTRY_USER/PASSWORD for the build stage (Standard)
    - podman login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    - podman build -t "$IMAGE_TAG" .
    - podman push "$IMAGE_TAG"

compliance_scan:
  stage: security_scan
  image: quay.io/podman/stable
  script:
    # Use --creds directly to bypass common authfile persistence issues in 403 scenarios
    - podman pull --creds "$CI_REGISTRY_USER:$CI_REGISTRY_PASSWORD" "$IMAGE_TAG"
    
    # Run security audits
    - podman run --rm "$IMAGE_TAG" ls /usr/lib/bootc/kargs.d/01-fips.toml
    - podman run --rm "$IMAGE_TAG" oscap-chroot / /usr/share/xml/scap/ssg/content/ssg-rhel9-ds.xml --profile pci-dss --report audit_report.html || true
  artifacts:
    paths:
      - audit_report.html

sign_os_image:
  stage: sign
  image: alpine:latest
  script:
    - apk add --no-cache cosign
    - echo "$COSIGN_KEY" > cosign.key
    # Cosign MUST also login to push the signature back to the registry
    - cosign login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    - cosign sign --key cosign.key --tlog-upload=false "$IMAGE_TAG"
  only:
    - main
